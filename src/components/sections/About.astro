---
/* About Section
   Two-column layout introducing PG
   Features:
   - Left: Image placeholder
   - Right: Bio text
   - Card design with cyan glow border
   - Responsive: stacks vertically on mobile
*/
---

<section class="about-section section">
  <div class="container">
    <h2 class="section-heading">About PG</h2>

    <div class="about-card">
      <div class="about-content">
        <!-- Image placeholder column -->
        <div class="about-image">
          <div class="image-placeholder">
            <span class="placeholder-icon">🎮</span>
            <p class="placeholder-text">Your Image Here</p>
          </div>
        </div>

        <!-- Bio text column -->
        <div class="about-text">
          <p>
            Hey! I'm PG, your guide through challenging worlds and epic adventures.
            I specialize in soulslikes, RPGs, and discovering hidden indie gems.
            Join me as we tackle tough bosses, explore rich stories, and find those
            games that deserve more love.
          </p>
        </div>
      </div>
    </div>
  </div>
</section>

<style>
  /* Section styling */
  .about-section {
    background-color: var(--bg-primary);
  }

  /* Section heading - centered with gradient */
  .section-heading {
    text-align: center;
    margin-bottom: var(--spacing-xl);
    background: linear-gradient(135deg, var(--accent-cyan), var(--accent-purple));
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
    background-clip: text;
  }

  /* Card container with cyan glow border */
  .about-card {
    background-color: var(--bg-surface);
    border: 2px solid var(--accent-cyan);
    border-radius: var(--radius-lg);
    padding: var(--spacing-xl);

    /* Subtle cyan glow effect */
    box-shadow: 0 0 20px rgba(0, 217, 255, 0.25);
  }

  /* Two-column layout using CSS Grid */
  .about-content {
    display: grid;
    grid-template-columns: 1fr 1fr; /* Two equal columns */
    gap: var(--spacing-xl);
    align-items: center;
  }

  /* Image placeholder styling */
  .about-image {
    width: 100%;
    height: 100%;
    min-height: 300px;
    display: flex;
    align-items: center;
    justify-content: center;
  }

  .image-placeholder {
    width: 100%;
    height: 100%;
    background: linear-gradient(135deg,
      rgba(0, 217, 255, 0.1),
      rgba(157, 78, 221, 0.1));
    border: 2px dashed var(--accent-purple);
    border-radius: var(--radius-md);
    display: flex;
    flex-direction: column;
    align-items: center;
    justify-content: center;
    gap: var(--spacing-sm);
    transition: background var(--transition-normal);
  }

  .image-placeholder:hover {
    background: linear-gradient(135deg,
      rgba(0, 217, 255, 0.15),
      rgba(157, 78, 221, 0.15));
  }

  .placeholder-icon {
    font-size: 4rem;
  }

  .placeholder-text {
    color: var(--text-secondary);
    font-size: 1rem;
    margin: 0;
  }

  /* Bio text styling */
  .about-text {
    display: flex;
    flex-direction: column;
    justify-content: center;
  }

  .about-text p {
    color: var(--text-primary);
    font-size: 1.125rem;
    line-height: 1.8;
    margin: 0;
  }

  /* Tablet breakpoint - adjust padding and spacing */
  @media (max-width: 768px) {
    .about-card {
      padding: var(--spacing-lg);
    }

    .about-content {
      gap: var(--spacing-lg);
    }

    .about-text p {
      font-size: 1rem;
    }

    .section-heading {
      margin-bottom: var(--spacing-lg);
    }
  }

  /* Mobile breakpoint - stack vertically */
  @media (max-width: 480px) {
    .about-content {
      grid-template-columns: 1fr; /* Single column - stack vertically */
      gap: var(--spacing-md);
    }

    .about-card {
      padding: var(--spacing-md);
    }

    .about-image {
      min-height: 200px;
    }

    .placeholder-icon {
      font-size: 3rem;
    }

    .about-text p {
      font-size: 0.95rem;
      line-height: 1.6;
    }

    .section-heading {
      margin-bottom: var(--spacing-md);
    }
  }
</style>
